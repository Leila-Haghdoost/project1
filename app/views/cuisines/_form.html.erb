<%= form_for @cuisine,html: {multipart: true} do |f| %>

<fieldset>
  <%= f.label :name %>
  <%= f.text_field :name %>
</fieldset>
<fieldset>
  <%= f.label :image %>
  <%= f.cl_image_upload :image %>
</fieldset>

<%= f.submit %>
<% end %>
